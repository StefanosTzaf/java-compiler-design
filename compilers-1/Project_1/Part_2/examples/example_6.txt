empty_test(x) {
    if ("" prefix x)
        "Always true!"
    else
        "Impossible"
}

wrap(x) {
    "[" + x + "]"
}

combine(a, b) {
    a + "-" + b
}

complex_call() {
    combine(wrap("left"), wrap(combine("middle", "core")))
}

complex_call()
empty_test("")